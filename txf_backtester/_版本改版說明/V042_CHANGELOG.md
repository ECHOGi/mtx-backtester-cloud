# V042_CHANGELOG.md

## v0.4.2：策略投放箱版

### 本版目的
讓平台可直接讀取 Google Drive 同步資料夾中的批次策略 JSON，不必透過 Codex，也不必用瀏覽器手動上傳。

### 新增
- 新增策略投放箱路徑：
  `G:\我的雲端硬碟\MTX Test Record\_策略投放箱`
- 側欄「批次策略回測」新增「策略投放箱」區塊。
- 平台會列出投放箱中的 `.json` 檔。
- 可按「讀取投放箱策略 JSON」載入批次策略。

### 未改動
- 未改交易邏輯。
- 未改斷頭強制平倉規則。
- 未改 correctness.py。
- 未改既有批次 JSON 格式。

